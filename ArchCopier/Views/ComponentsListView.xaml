<UserControl x:Class="ProductComposition.Views.ComponentsListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:interfaces="clr-namespace:ProductComposition.Models.Interfaces"
             d:DataContext="{x:Type interfaces:IComponentCollection}"
             mc:Ignorable="d"
             d:DesignHeight="220" d:DesignWidth="450">

    <Grid>
        <DockPanel LastChildFill="True" Margin="0 0 0 0">
            <GroupBox DockPanel.Dock="Top" Width="auto" Height="auto" Header="Список компонентов сборки">
                <ListBox ItemsSource="{Binding ComponentList}" DisplayMemberPath="ComponentName"
                         SelectedItem="{Binding SelectedComponent, Mode=TwoWay}">
                </ListBox>
            </GroupBox>
        </DockPanel>
    </Grid>
</UserControl>